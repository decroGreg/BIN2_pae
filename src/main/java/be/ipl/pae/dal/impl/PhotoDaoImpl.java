package be.ipl.pae.dal.impl;

import be.ipl.pae.dal.interfaces.PhotoDao;

public class PhotoDaoImpl implements PhotoDao {
  /**
   * private PreparedStatement ps; private DaoServices services; private Factory factory;
   * 
   * 
   * /** Constructeur Photo Dao.
   * 
   * @param daoService classe service.
   * 
   *        public PhotoDaoImpl(DaoServices daoService) { this.services = daoService; this.factory =
   *        new FactoryImpl(); }
   * 
   * 
   *        public boolean createPhoto(PhotoDto photoDto){ String requeteSql = "INSERT INTO
   *        init.photos VALUES(DEFAULT, ?, ?, ?, ?)"; ps = services.getPreparedSatement(requeteSql);
   *        try {
   * 
   *        }
   * 
   *        }
   **/
}
